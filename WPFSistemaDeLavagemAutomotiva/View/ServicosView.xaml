<Page x:Class="WPFSistemaDeLavagemAutomotiva.View.ServicosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFSistemaDeLavagemAutomotiva.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServicosView">

    <Border Background="#1C1C1C" CornerRadius="20">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" MinHeight="120"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"  Text="Serviços" FontSize="35" Height="Auto" FontWeight="Bold" Foreground="#ECEFF1" Margin="10,10,0,5"/>
            <Border CornerRadius="10" Background="#28a745" Width="170" Height="43" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,25,10">
                <Button x:Name="btnAdicionarServicos" Grid.Row="0" Content="Adicionar Serviço" Width="150" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold"/>
            </Border>
            <TextBlock x:Name="tbTotalServicos" Grid.Row="1" Text="Total de Serviços: " FontSize="18" FontWeight="SemiBold" Foreground="#ECEFF1" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" Text="Pesquisar:" FontSize="16" FontWeight="Normal" Foreground="#ECEFF1" Margin="0,0,230,0" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Width="200" Height="30" HorizontalAlignment="Right" Margin="0,0,20,0" VerticalAlignment="Center"/>

            <DataGrid x:Name="dgServicos"
                        Grid.Row="2"
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        CanUserAddRows="False"
                        SelectionMode="Single"
                        Background="#1C1C1C"
                        Foreground="#E0E0E0"
                        AlternatingRowBackground="#2E2E2E"
                        RowBackground="#252525"
                        GridLinesVisibility="All"
                        HeadersVisibility="Column"
                        RowHeight="30"
                        Margin="10"
                        BorderThickness="0"
                        >

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#026ADE"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#252525"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1A73E8"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#0240DE"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>


                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding IdServico}" Width="50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Serviços" Binding="{Binding NomeServico}" Width="*"/>
                    <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="120"/>
                    <DataGridTextColumn Header="Ativo" Binding="{Binding Ativo}" Width="120"/>
                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Background="Yellow"  Width="30" Margin="5 0"/>
                                    <Button Content="🗑️" Background="Red" Width="30" Margin="5 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>


    </Border>
</Page>
