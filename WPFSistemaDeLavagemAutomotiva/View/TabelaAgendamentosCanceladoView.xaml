<Page x:Class="WPFSistemaDeLavagemAutomotiva.View.TabelaAgendamentosCanceladoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFSistemaDeLavagemAutomotiva.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TabelaAgendamentosCanceladoView">

    <Grid>
        <Grid Background="#1C1C1C">
            <DataGrid x:Name="dgAgendamentosCancelado"
                      Grid.Row="2"
                      AutoGenerateColumns="False"
                      Background="#1C1C1C"
                      Foreground="#E0E0E0"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      AlternatingRowBackground="#2E2E2E"
                      RowBackground="#252525"       
                      GridLinesVisibility="All"
                      HeadersVisibility="Column"
                      RowHeight="30"
                      Margin="10"
                      BorderThickness="0"
                      > 

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#026ADE"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#252525"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1A73E8"/>
                        </Trigger> 
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#0240DE"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>



                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding IdAgendamento}" Width="30">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteAgendado.Nome}" Width="*"/>
                    <DataGridTextColumn Header="Serviço" Binding="{Binding ServicoAgendado.NomeServico}" Width="*"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataAgendada, StringFormat={}{0:dd/MM/yyyy}}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Hora" Binding="{Binding HoraAgendamento}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Valor" Binding="{Binding ServicoAgendado.Valor, StringFormat=C}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Background="Yellow"  Width="30" Margin="5 0"/>
                                    <Button Content="🗑️" Background="Red" Width="30" Margin="5 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
