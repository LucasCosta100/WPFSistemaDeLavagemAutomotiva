<Page x:Class="WPFSistemaDeLavagemAutomotiva.View.FuncionariosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFSistemaDeLavagemAutomotiva.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FuncionariosView">

    <Border Background="#1C1C1C" CornerRadius="20">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" MinHeight="120"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"  Text="Funcionários" FontSize="35" Height="Auto" FontWeight="Bold" Foreground="#ECEFF1" Margin="10,10,0,5"/>
            <TextBlock x:Name="tbTotalFuncionarios" Grid.Row="1" Text="Total de funcionários: " FontSize="18" FontWeight="SemiBold" Foreground="#ECEFF1" Margin="10,0,0,0" VerticalAlignment="Center"/>
            

            <DataGrid x:Name="dgFuncionario"
                       Grid.Row="2"
                       AutoGenerateColumns="False"
                       IsReadOnly="True"
                       CanUserAddRows="False"
                       SelectionMode="Single"
                       Background="#1C1C1C"
                       Foreground="#E0E0E0"
                       AlternatingRowBackground="#2E2E2E"
                       RowBackground="#252525" 
                       GridLinesVisibility="All"
                       HeadersVisibility="Column"
                       RowHeight="30"
                       Margin="10"
                       BorderThickness="0"
                   >

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#026ADE"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#252525"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1A73E8"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#0240DE"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>



                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding IdFuncionario}" Width="30">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Cargo" Binding="{Binding Cargo}" Width="*"/>
                    <DataGridTextColumn Header="Rua" Binding="{Binding Endereco.Rua}" Width="*"/>
                    <DataGridTextColumn Header="Número" Binding="{Binding Endereco.Numero}" Width="*"/>
                    <DataGridTextColumn Header="Cidade" Binding="{Binding Endereco.Cidade}" Width="*"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Endereco.Estado}" Width="50"/>
                    <DataGridTextColumn Header="CEP" Binding="{Binding Endereco.Cep}" Width="*"/>
                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="btnEditarFuncionario" Content="✏️" Background="Yellow"  Width="30" Margin="5 0" Tag="{Binding}" Click="btnEditarFuncionario_Click"/>
                                    <Button x:Name="btnDesativarFuncionario" Content="🗑️" Background="Red" Width="30" Margin="5 0" Tag="{Binding}" Click="btnDesativarFuncionario_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>


    </Border>
</Page>
