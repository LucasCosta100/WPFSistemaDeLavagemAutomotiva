<Page x:Class="WPFSistemaDeLavagemAutomotiva.View.FuncionariosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFSistemaDeLavagemAutomotiva.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FuncionariosView">

    <Border Background="#eff2f7" CornerRadius="20">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" MinHeight="120"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"  Text="Funcionários" FontSize="35" Height="Auto" FontWeight="Bold" Foreground="#333" Margin="10,10,0,5"/>
            <Border CornerRadius="10" Background="#28a745" Width="170" Height="43" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,25,10">
                <Button x:Name="btnAdicionarFuncionario" Grid.Row="0" Content="Adicionar Funcionário" Width="150" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold"/>
            </Border>
            <TextBlock x:Name="tbTotalFuncionarios" Grid.Row="1" Text="Total de funcionários: " FontSize="18" FontWeight="SemiBold" Foreground="#555" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" Text="Pesquisar:" FontSize="16" FontWeight="Normal" Foreground="#555" Margin="0,0,230,0" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Width="200" Height="30" HorizontalAlignment="Right" Margin="0,0,20,0" VerticalAlignment="Center"/>

            <DataGrid x:Name="dgFuncionario"
                   Grid.Row="2"
                   AutoGenerateColumns="False"
                   IsReadOnly="True"
                   CanUserAddRows="False"
                   SelectionMode="Single"
                   AlternatingRowBackground="#E8ECFF"
                   GridLinesVisibility="All"
                   HeadersVisibility="Column"
                   RowHeight="30"
                   Margin="10"
                   BorderThickness="0"
                   >

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#026ADE"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#D0E0FF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#0240DE"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>



                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding IdFuncionario}" Width="50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Cargo" Binding="{Binding Cargo}" Width="*"/>
                    <DataGridTextColumn Header="Rua" Binding="{Binding Endereco.Rua}" Width="*"/>
                    <DataGridTextColumn Header="Número" Binding="{Binding Endereco.Numero}" Width="*"/>
                    <DataGridTextColumn Header="Cidade" Binding="{Binding Endereco.Cidade}" Width="*"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Endereco.Estado}" Width="50"/>
                    <DataGridTextColumn Header="CEP" Binding="{Binding Endereco.Cep}" Width="*"/>
                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Background="Yellow"  Width="30" Margin="5 0"/>
                                    <Button Content="🗑️" Background="Red" Width="30" Margin="5 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>


    </Border>
</Page>
