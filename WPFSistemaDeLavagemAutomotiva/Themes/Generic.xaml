<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPFSistemaDeLavagemAutomotiva.View.Components">


    <Style TargetType="{x:Type local:NavButton}">

        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NavButton}">
                    <Border Name="back"  CornerRadius="8" Padding="{TemplateBinding Padding}" Background="Transparent" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Path Name="icon" Height="25" Width="25" Stretch="Uniform" Fill="{TemplateBinding IconFill}" Data ="{TemplateBinding Icon}"/>
                            <TextBlock Name ="text" Margin="25,0,0,0" Text="{TemplateBinding NavText}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Triggers para eventos como passar o mouse por cima do item (hover) -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" TargetName="back" Value="LightGray"></Setter>
                            <Setter Property="BorderThickness" TargetName="back" Value="1"></Setter>
                            <Setter Property="Fill" TargetName="icon" Value="{Binding HoverFill, RelativeSource={RelativeSource TemplatedParent}}"></Setter>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#0D47A1"></Setter>
                            <Setter Property="Fill" TargetName="icon" Value="{Binding SelectedFill, RelativeSource={RelativeSource TemplatedParent}}"></Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
